export function HomeScene(k) {
    k.scene("home", () => {
        const title = k.add([
            k.text("XFS350", {
                font: "arial",
                size: 50
            })
        ]);
    });
};